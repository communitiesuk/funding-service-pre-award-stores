# Stuff that needs doing in this repo
- Make the DB migrations use the paketo image that was just built (not build fresh image)
- Sort out run-db-migrations script and naming (had to override service name)
